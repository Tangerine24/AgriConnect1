{
  "name": "agriconnect-unified",
  "version": "1.0.0",
  "description": "AgriConnect Precision Agritech Market Linkage Platform",
  "main": "backend/src/server.ts",
  "scripts": {
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm install && npm run build",
    "build:backend": "cd backend && npm install && npx prisma db push --accept-data-loss && npx ts-node prisma/seed.ts",
    "start": "cd backend && npx ts-node --transpile-only src/server.ts"
  },
  "keywords": ["agritech", "sih", "market-linkage"],
  "author": "AgriConnect Team",
  "license": "ISC"
}
